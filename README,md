# Poster Generator
Brand new poster generator for Urlab's Smartmondays

## Installation 🚀
```
pip install poetry
poetry install
poetry shell
# Run the program
python3 main.py
# Exit poetry shell
exit
```
